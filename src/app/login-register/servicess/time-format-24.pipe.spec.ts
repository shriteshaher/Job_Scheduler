import { TimeFormat24Pipe } from './time-format-24.pipe';

describe('TimeFormat24Pipe', () => {
  it('create an instance', () => {
    const pipe = new TimeFormat24Pipe();
    expect(pipe).toBeTruthy();
  });
});
